import type { ManageMediaModelDefinition } from './types'

export const MANAGE_MEDIA_MODEL: ManageMediaModelDefinition = {
  id: 'utility:manage-media',
  label: 'Manage Media',
  description:
    'Upload frequently-used images to your Runware account so they can be quickly inserted as input anywhere in the generator, without re-uploading each time.',
  taskType: 'manageMedia',
}
